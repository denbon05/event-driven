SERVER_PORT={{user.port}}
POSTGRES_PASSWORD={{user.db_pass}}
POSTGRES_PORT={{user.db_port}}
POSTGRES_USER={{user.db_user}}
POSTGRES_DB={{user.db_name}}
DATABASE_URL={{user.db_url}}
KAFKA_BROKER_URI={{user.kafka_broker_uri}}
